# URL base da API (Opção A — GitHub Pages, somente leitura).
# Já existe um padrão no código; defina isto apenas se quiser apontar para
# outra fonte (ex.: json-server local da Opção B em http://localhost:3001).
NEXT_PUBLIC_API_BASE_URL=https://ThiagoLifters.github.io/api_test
